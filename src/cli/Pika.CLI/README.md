# Pika.CLI

Command-line interface for the Pika SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global Pika.CLI --prerelease
```

## Usage

```bash
pika --help
pika api --help
```